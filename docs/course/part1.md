Реализовать:

1. Функции арифметических операций для массива чисел с использованием традиционного подхода с вложенными циклами.
2. Функции арифметических операций для массива чисел с использованием SIMD(можно не напрямую, а используя библиотеки)(уровень абстракции используемой библиотеки влияет на оценку).
3. п.1 и п.2 желательно реализовать с использованием обобщенного подхода(шаблоны С++, callable объекты) с минимальным дублированием кода. 
4. Функцию суммы(reduction) элементов массива.
5. Функцию активации ReLU, Sigmoid(Для  SIMD потребуется аппроксимация), SiLU.
6. Тесты на корректность(можно использовать п.1 как опорный).
7. Нагрузочные тесты для двух реализаций(традиционный, SIMD), для разного размера данных и типов данных.

Арифметические операции: +, -, *, /.
Типы данных: int32, float32, float64.
Использование параллелизма будет плюсом.

Составить отчет с выводом какая из реализаций для какого типа  и размера данных производительней(Предпочтительней в виде jupyter notebook с графиками).

 Максим 4 бала за задание.